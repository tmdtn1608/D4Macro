<Window x:Class="D4Macro.SettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:D4Macro"
        xmlns:vm="clr-namespace:D4Macro.ViewModel"
        xmlns:m="clr-namespace:D4Macro.Model"
        xmlns:u="clr-namespace:D4Macro.Util"
        mc:Ignorable="d"
        Title="Config" Height="330" Width="330">
    <Window.DataContext>
        <m:ConfigModel/>
    </Window.DataContext>
    <Window.Resources>
        <u:KeyConverter x:Key="KeyConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding LaunchKey, Converter={StaticResource KeyConverter}}"/>
        
        <TextBlock Grid.Row="1" Text="{Binding ActiveTray}"/>
    </Grid>
</Window>
