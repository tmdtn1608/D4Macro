<Window x:Class="D4Macro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:D4Macro"
        xmlns:view="clr-namespace:D4Macro.View"
        mc:Ignorable="d"
        ResizeMode="CanMinimize"
        Title="D4Macro" Height="300" Width="330"
        Icon="Resources/devil.ico"
        StateChanged="Window_StateChanged"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Menu bar -->
            <RowDefinition Height="*"/> <!-- Main content -->
        </Grid.RowDefinitions>
        <Menu Background="White">
            <MenuItem Header="_Edit">
                <!-- <MenuItem Command="ApplicationCommands.Copy"/> -->
                <!-- <MenuItem Command="ApplicationCommands.Cut"/> -->
                <!-- <MenuItem Command="ApplicationCommands.Paste"/> -->
                <MenuItem Header="Import" Click="Import_Setting"/>
                <MenuItem Header="Export" Click="Export_Setting"/>
                <MenuItem Header="Config" Click="SetConfig"/>
                <MenuItem Header="Exit" Click="ApplicationTerminate"/>
            </MenuItem>
        </Menu>
        <view:MainView Grid.Row="1" DataContext="{Binding }"/>
    </Grid>
</Window>
